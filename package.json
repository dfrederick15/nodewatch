{
  "name": "supermon-ts",
  "version": "1.0.0",
  "description": "AllStar node monitor and manager — TypeScript rewrite",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types server.ts",
    "dev": "node --experimental-strip-types --watch server.ts"
  },
  "dependencies": {
    "smol-toml": "^1.3.1"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
